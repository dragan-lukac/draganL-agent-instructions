# Session Summary — 2026-04-13

## What we did

### 1. Plex Media Server update
- Identified that the downloaded file was `i386` (32-bit) but the system is `amd64` (64-bit)
- Advised downloading the correct `amd64` `.deb` from plex.tv

### 2. TTS Voice Scripts — keyboard shortcuts
- Updated `TTSforAI/linux/monitor.sh` to properly kill the full piper/aplay process group
- Created `TTSforAI/linux/stop.sh` — stops speech immediately
- Created `TTSforAI/linux/read-selection.sh` — reads highlighted text aloud
- Installed `xdotool` and `xclip`
- Registered shortcuts in Ubuntu Settings:
  - `Ctrl+Alt+S` → stop speech
  - `Ctrl+Alt+R` → read selected text
- Fixed a bug where selection was lost before the script grabbed it (added `sleep 0.3`)
- Fixed stdin vs argument bug in speak-md.sh pipe

### 3. Notion integration
- Created `scripts/notion.py` — Notion API helper (list databases, list pages, get page, search, create page)
- Installed `notion-client` Python package
- Token stored in `scripts/.env` (gitignored)
- Root HUB page ID: `341a338b6c51809b8fe1d17ede7e28dc`

### 4. HUB → Notion migration
- Created `scripts/migrate_to_notion.py`
- Migrated full HUB structure to Notion under "HUB" page:
  - **DaHab** — 2 docs
  - **Ideje** → Brate ti si lud (3 docs), Realne (5 docs)
  - **Ljudi** — 1 doc
  - **Dokumentacija** → Muzika (2 html files)
  - **Music** → CAT_FAT (YouTube: @CAT_FAT_music), SlobodaZaSve (YouTube: @SloZaSe), JammingSessions (sessions by date)
  - **Projekti** → circuitQ (GitHub), draganL-agent-instructions (GitHub), TTS for AI

### 5. Apple Notes → Notion (in progress)
- Created `scripts/scrape_apple_notes.py` — Playwright-based scraper
- Opens iCloud.com in a browser, user logs in manually
- Uses `/tmp/tts_notes_ready` signal file to trigger scraping (workaround for no interactive terminal)
- Target: **Pasme** folder in Apple Notes
- Status: scraper runs but page renders too slowly — fix applied (waits for spinner + 4s delay), needs re-test

## Scripts created this session
| File | Purpose |
|---|---|
| `scripts/.env` | Notion token + root page ID (gitignored) |
| `scripts/notion.py` | Notion API helper CLI |
| `scripts/migrate_to_notion.py` | HUB → Notion full migration |
| `scripts/scrape_apple_notes.py` | Apple Notes Pasme → Notion via Playwright |
| `TTSforAI/linux/stop.sh` | Stop TTS speech (Ctrl+Alt+S) |
| `TTSforAI/linux/read-selection.sh` | Read selected text (Ctrl+Alt+R) |

## Pending
- [ ] Re-test Apple Notes scraper (iCloud page needs to fully load before signal)
- [ ] Verify Notion HUB pages look correct
- [ ] Rotate Notion API token (was shared in plain text during session)
